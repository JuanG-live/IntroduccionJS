// Manipulation with JS elements

const heading = document.querySelector('.heading')
const url = document.querySelectorAll('.navegacion a')

heading.textContent = 'Nuevo ID'
heading.id = 'No TENGO IDEA QUE estoy haciendo'