const count = 500
const pay = 1000
const target = false

/**
 * 
 * Logical OR and Logical AND
 * || or 
 * && and 
 * 
 */

if (pay > count || target) {
    console.log('You can pay')
} else {
    console.log('You cannot pay')
}